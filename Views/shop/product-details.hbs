<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shop</title>
    <style>
    .product-list {
            display: flex;
            justify-content: space-around;
            border: 1px solid black;
            padding: 10px;
            width: 80%;
            margin: auto;
        }

        .my-product-image{
            width: 100%; /* Make sure the image takes up the full width of its container */
            height: auto;
        }
        .product-item {
            border: 1px solid black;
            padding: 5px;
            border-radius: 5px;
            box-shadow: 5px 10px 10px #ccc;
            width: 200px; /* Set the width of each product item */
            margin: 10px; /
        }

        .category {
            width: 100%;
        }

        .product-img{
             width: 100%; /* Ensure the image fills its container */
    `       height: auto; /* Allow the height to adjust proportionally */
            display: block; 
        }
    </style>
    {{>generalcss}}
</head>

<body>

    {{>navbar}}
    <h1>Welcome to CB Shop</h1>

    <div class="product-item">
        <div class="my-product-image">
            <img class="product-img" src={{product.imageUrl}}>
        </div>
        <br>
        <h4>Product Name: {{product.name}}</h4>
        <h4>Product Price: {{product.price}}</h4>
        <h4>Product Seller: {{product.seller}}</h4>
        <h4>Product Description: {{product.description}}</h4>
        <h4>Reviews:</h4>
        <ul>
            {{#each product.review as |r|}}
            <li>{{r}}</li>
            {{/each}}
        </ul>
    </div>

    <a href="/shop/cart/add/{{product._id}}">
        <button>Add To Cart</button>
    </a>
    <a href="#">
        <button>Buy Now</button>
    </a>
</body>

</html>